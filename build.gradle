apply plugin: 'java'
apply plugin: 'application'

sourceCompatibility = 1.8
version = '0.1'

sourceSets {
    main {
        java {
            srcDirs = ['src']
        }
    }
}

dependencies {
    compile files('lib/htmllexer.jar', 'lib/htmlparser.jar')
}

mainClassName = "pcminer.PCMiner"


